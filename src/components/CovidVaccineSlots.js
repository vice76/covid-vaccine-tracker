import React, { useState } from "react";
import axios from "axios";

export default class CovidVaccineSlots extends React.Component {
  state = {
    VaccineLocation: [],
    flag: "false",
  };
  
  render() {
    return <></>;
  }
}
